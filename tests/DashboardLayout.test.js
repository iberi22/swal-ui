import { describe, it, expect } from 'vitest';
import DashboardLayout from '../src/layouts/DashboardLayout.svelte';

describe('DashboardLayout Layout Component', () => {
  it('is a valid Svelte component', () => {
    expect(DashboardLayout).toBeDefined();
  });
});
